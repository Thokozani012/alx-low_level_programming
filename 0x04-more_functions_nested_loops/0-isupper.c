#include "main.h"

/**
 * main - Function checks for uppercase character.
 *
 * Retun: 1 if character c is uppercase
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'B')
	{
		return (1);
	}
	return (0);
}
