#include "main.h"

/**
 * _islower - checks if c is a lowercase letter
 * @c: The parameter to be checked.
 *
 * Return: 1 if c is a lowercase and 0 otherwise.
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
