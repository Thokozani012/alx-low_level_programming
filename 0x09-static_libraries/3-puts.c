#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _puts - prints a string to the stdout
 * @str: string to be printed
 *
 * Return: Nothing (void function)
 */

void _puts(char *str)
{
	puts(str);
}
